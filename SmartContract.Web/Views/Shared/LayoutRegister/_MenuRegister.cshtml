
@inject SmartContract.Infrastructure.Interfaces.Authenticate.IUserService UserService
@using SmartContract.Utils

@{
    Layout = null;

    var username = UserService.GetUserName();

    var _ControllerSMCT = String.Empty;

    if (GroupRoles.GroupRoleUnit.Contains(UserService.GetRoleRoleCode()))
        _ControllerSMCT = "HomeVendor";
    else
        _ControllerSMCT = "Home";

}

<style>
</style>


<nav class="navbar navbar-expand-lg navbar-light" style="
        background-color: rgba(255, 255, 255, 0.5);
        /*border-bottom: 1px solid gray;*/
        box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2), 0 6px 20px 0 rgba(0, 0, 0, 0.1);
">
    <div class="container">
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarsSmart01" aria-controls="navbarsSmart01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        @if (String.IsNullOrEmpty(username))
        {
            <a class="navbar-brand" asp-controller="Authorize" asp-action="LogOut" >
                <h4>
                    <b><span class="text-primary">Smart</span>Contract </b><span style="font-size:medium">จัดการผู้ใช้งาน </span>
                </h4>
            </a>
        }
        else
        {
            <a class="navbar-brand">
                <h4>
                    <b><span class="text-primary">Smart</span>Contract </b><span style="font-size:medium">จัดการผู้ใช้งาน </span>
                </h4>
            </a>
        }

        @if (!String.IsNullOrEmpty(username))
        {
            <div class="collapse navbar-collapse justify-content-end " id="navbarsSmart01">
                <ul class="navbar-nav">
                    <li class="nav-item active">

                        <a class="nav-link text-primary" onclick="showLoading()"
                           asp-controller="@_ControllerSMCT" asp-action="Index">เข้าโปรแกรม SmartContract</a>

                    </li>
                    @*<li class="nav-item active">
                        <a class="nav-link" onclick="showLoading()"
                           asp-controller="Register" asp-action="Profile">Profile</a>
                    </li>*@
                </ul>
            </div>

            <div class="justify-content-end navbar-toggle-box-collapse">
                <ul class="navbar-nav">
                    <li class="nav-item active">
                        <a class="nav-link" asp-area="" asp-controller="Authorize" asp-action="LogOut">ออกจากระบบ <span class="sr-only">(current)</span></a>
                    </li>
                </ul>
            </div>
        }
    </div>
</nav>

